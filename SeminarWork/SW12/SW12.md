

### Задание 1. Метрики качества: IoU, Precision–Recall и mAP

1. Реализуйте на Python функцию

   ```python
   def compute_iou(box_a, box_b):
       # box = (xmin, ymin, xmax, ymax)
       ...
   ```

   возвращающую IoU двух прямоугольников.
2. Сгенерируйте случайные наборы «предсказанных» и «эталонных» боксов (по 100 каждого) в пределах изображения 640×480.
3. Для порогов IoU = {0.3, 0.5, 0.7} постройте кривые Precision–Recall.
4. Реализуйте вычисление AP@τ (по определению площади под PR-кривой) и усреднённое mAP₀.₅:₀.₉₅ (с шагом 0.05).
5. Проанализируйте, как меняется mAP при росте ложных срабатываний (добавьте шумовые боксы) и при сдвиге истинных боксов (искажение координат).

---

### Задание 2. Сравнение скользящего окна и Region Proposal Network

1. Реализуйте простой «скользящий» детектор: на изображении 224×224 по сетке с шагом 32 и трём размерам окон {64, 96, 128} вычисляйте гипотетическую «уверенность» через сумма-абсолютных-разностей пикселей с шаблоном.
2. Посчитайте общее число окон и время «инференса» (измерьте `time.time()` в Python).
3. На основе PyTorch создайте мини-RPN: одну 3×3-свёртку с K=9 якорями, голову objectness (логиты) и голову регрессии.
4. Для случайного тензора признаков 56×56×256 замерьте число генерируемых предложений и время прохождения RPN-блока.
5. Сравните обе схемы по числу кандидатов и скорости, оформите вывод в виде таблицы.

---

### Задание 3. Реализация ROI Align и Fast R-CNN-головы

1. На PyTorch реализуйте функцию `roi_align(feature_map, rois, output_size)` по описанию (2×2 точки на каждый бин, билинейная интерполяция).
2. Проверьте корректность на простом примере: скопируйте кусок feature\_map и сравните с вашим результатом.
3. Постройте «Fast R-CNN-голову»: два FC-слоя (по 1024) + классификатор (C+1) + регрессор (4 координаты на класс).
4. Для случайного feature\_map размером 14×14×512 и 100 RoI протестируйте end-to-end (roi\_align → head) и замерьте время.
5. Сделайте небольшой бенчмарк: сравните ваш roi\_align с `torchvision.ops.roi_align` по скорости и точности (максимальная разница на входных тестах).

---

### Задание 4. Одношаговые детекторы: сравнение SSD и YOLO-стиля

1. Реализуйте упрощённую версию SSD-головы на одной feature-карте 38×38 с K=6 якорями и C=3 классами (простая регрессия и классификация).
2. Реализуйте YOLO-блок: разделение входа 7×7, две bbox на клетку, сигмоиды для центра и класса.
3. Создайте synthetic-датасет: на чёрном фоне случайные белые прямоугольники трёх размеров.
4. Обучите оба детектора (по 20 эпох, Adam, lr=1e-3) на этом датасете и сравните по mAP₀.₅ и FPS.
5. Напишите краткий отчёт: какие преимущества/недостатки каждой схемы проявились в вашем эксперименте.

---

### Задание 5. Anchor-free детектор FCOS из “нулевого” прототипа

1. Реализуйте на PyTorch модуль `FCOSHead`, который для каждого пикселя feature\_map предсказывает четыре расстояния (l, r, t, b) и класс-логит.
2. Добавьте стратификацию по масштабам: назначьте каждому пикселю уровень в зависимости от того, в какой из четырёх feature\_map-решений он лежит.
3. Сгенерируйте пара искусственных изображений с геометрически расположенными прямоугольниками и обучите «прототип» FCOS на 10 эпох.
4. Визуализируйте предсказанные рамки на примерах, оцените IoU среднее и AR (recall при 100 предсказаниях).
5. Проанализируйте, как точность меняется при добавлении в задачу «фона» (наложение шумовых точек), и сделайте выводы о устойчивости anchor-free схем.

---
