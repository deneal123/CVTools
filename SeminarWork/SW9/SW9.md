### **Задание 1. Сравнение параметров и FLOPs для малых и больших ядер**  
1. Напишите на Python функцию `compute_flops(cin, cout, H, W, kernel_sizes)`, которая по входным каналам `cin`, выходным `cout`, пространственным размерностям `H`, `W` и списку размеров ядер `kernel_sizes` возвращает число параметров и FLOPs для каждого ядра.  
2. С помощью этой функции вычислите параметры и FLOPs для одного $5×5$ сверточного слоя и двух последовательных $3×3$ слоёв при  
   $
     C_{\text{in}}=64,\;C_{\text{out}}=128,\;H=W=56.
   $  
3. Выведите в консоль процентную разницу по параметрам и FLOPs между схемами.

---

### **Задание 2. Реализация и анализ Residual-блока на MNIST**  
1. На PyTorch реализуйте класс `ResidualBlock` из лекции (две последовательные $3×3$ свёртки с BatchNorm + ReLU и skip-connection).  
2. Постройте две сети:  
   - с тремя такими `ResidualBlock` + двумя полносвязными слоями;  
   - такую же без skip-connections (удалив сложение $x + F(x)$).  
3. Обучите обе модели на MNIST (10 эпох, batch=64).  
4. Постройте и сравните графики loss и accuracy на train и validation.  
5. Проанализируйте, как наличие skip-connections влияет на скорость и стабильность обучения.

---

### **Задание 3. Интеграция SE-блока в простую CNN и оценка эффекта**  
1. Реализуйте на PyTorch модуль `SqueezeExcite` согласно разделу 9.1.  
2. Постройте две версии простой CNN (две $3×3$+ReLU → MaxPool → FC):  
   - без SE-блоков;  
   - с SE-блоком после каждой свёртки.  
3. Обучите обе модели на подмножестве CIFAR-10 (5 000 примеров) по 5 эпох.  
4. Измерьте точность на тесте и количество параметров каждой модели.  
5. Сравните качество и сделайте вывод о влиянии SE-модуля.

---

### **Задание 4. Калькулятор compound scaling для EfficientNet**  
1. Напишите функцию `compound_scale(phi, alpha, beta, gamma)`, возвращающую  
   $
     d = \alpha^\phi,\quad w = \beta^\phi,\quad r = \gamma^\phi.
   $  
2. Для $\alpha=1.2$, $\beta=1.1$, $\gamma=1.15$ вычислите $d,w,r$ при $\phi=0,1,2,3,4$.  
3. Оцените относительный рост FLOPs как  
   $\alpha^{2\phi}\,\beta^{2\phi}\,\gamma^{2\phi}.$  
4. На основе полученных значений определите, при каком $\phi$ модель остаётся в пределах ~1 GFLOPs.

---

### **Задание 5. Проектирование Inception-модуля под заданный бюджет каналов**  
1. Реализуйте функцию `design_inception(cin, budget)`, которая подбирает целочисленные параметры  
   $(F_1, F_{3\_reduce}, F_3, F_{5\_reduce}, F_5, F_{pool\_proj})$ так, чтобы  
   $
     F_1 + F_3 + F_5 + F_{pool\_proj} \;\le\; \text{budget}.
   $
2. Для $C_{\text{in}}=160$ и `budget=192` найдите хотя бы три различных конфигурации ветвей.  
3. Для каждой конфигурации рассчитайте общее число параметров Inception-модуля (с учётом редукции через $1×1$).  
4. Проанализируйте, какие ветви вносят наибольший вклад в число параметров, и сравните trade-off между ними.